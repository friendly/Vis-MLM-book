%%%%%%%%%%%%%%%  Start of preamble.tex %%%%%%%%%%%%%%%%%% 
%%%%%%%%%%%%%%%  --------------------- %%%%%%%%%%%%%%%%%%
\usepackage{pdfpages}  % for inserting cover page PDF
\usepackage{booktabs}
\usepackage{longtable}
\usepackage[bf,singlelinecheck=off]{caption}
\usepackage{url}

\usepackage{framed}
%\usepackage{color}  % is this needed?
%\definecolor{shadecolor}{RGB}{248,248,248}
%
% COLOR schemes -- CRC wants CMYK
\definecolor{shadecolor}{cmyk}{0,0,0,0.03}
\usepackage[cmyk]{xcolor} % CRC wants CMYK

% Define custom colors in CMYK (CRC requirement)
\definecolor{darkgreen}{cmyk}{0.98, 0, 0.36, 0.80}  % RGB(1,50,32)
\definecolor{mygreen}  {cmyk}{0.80, 0, 0.60, 0.50}  % RGB(26,127,51) ≈ #1A7F33

%%%% section headings %%%%


% https://tex.stackexchange.com/questions/750528/how-to-change-color-of-section-title-in-the-krantz-cls/750529#750529
\renewcommand\SectionHeadFont{\color{blue}\fontsize{12}{14}\sffamily\bfseries\selectfont}
\renewcommand\SubsectionHeadFont{\color{blue}\fontsize{11}{13}\sffamily\bfseries\selectfont}
\renewcommand\SubsubsectionHeadFont{\color{blue}\fontsize{10}{12}\sffamily\bfseries\selectfont}



%%%% Code block line-breaking %%%%
% fvextra extends fancyvrb with breaklines support (plain fancyvrb does not have it).
% Must be loaded after fancyvrb (which Pandoc loads in the generated preamble).
% breaklines=true wraps at spaces; breakanywhere=true also breaks mid-token if needed.
\usepackage{fvextra}
\RecustomVerbatimEnvironment{Highlighting}{Verbatim}{commandchars=\\\{\},breaklines=true,breakanywhere=true}

%%%% Page headers %%%%%%
% override krantz default to use chapter/section headers
\makeatletter
\def\HeadingsChapterSection{%
  \def\chaptermark##1{%
    \markboth{%
      ##1}{}}%
  \def\sectionmark##1{%
    \markright{%
      \thesection: ##1}}}
\HeadingsChapterSection


% block quotes
\definecolor{quotecolor}{cmyk}{1, 1, 0, 0.61} % RGB(0,0,100) — dark blue
\renewenvironment{quote}
{%
  \list{}{\rightmargin=\leftmargin}%
  \item\relax\color{quotecolor}% <-- Apply the color here
  \sffamily
}
{%
  \endlist
}

%--- Cover page
% Run:
%   pdftk A=images/cover-ellipse.pdf B=index.pdf cat A1 B2-end output index-cover.pdf
%   BUT: this changes the structure of the PDF-- no bookmarks, etc.,
%   What is needed is to open index.pdf and INSERT the cover as the first page.
%   pdftk A=index.pdf B=images/cover-ellipse.pdf cat A0 B1 A2-end output index-cover.pdf
%   --> Did this manually in Adobe Acrobat Pro WORKED

% floats
\renewcommand{\textfraction}{0.05}
\renewcommand{\topfraction}{0.8}
\renewcommand{\bottomfraction}{0.8}
\renewcommand{\floatpagefraction}{0.75}

% for tinytable / modelsummary
\usepackage{tabularray}
\usepackage{float}
\usepackage{graphicx}
\usepackage{rotating}
\usepackage[normalem]{ulem}
\UseTblrLibrary{booktabs}
\UseTblrLibrary{siunitx}
% \newcommand{\tinytableTabularrayUnderline}[1]{\underline{#1}}
% \newcommand{\tinytableTabularrayStrikeout}[1]{\sout{#1}}
% \NewTableCommand{\tinytableDefineColor}[3]{\definecolor{#1}{#2}{#3}}


% Make \href{} into a footnote --- does this work??
\let\oldhref\href
\providecommand{\href}[2]{#2\footnote{\url{#1}}}
  
% is this needed???
% \makeatletter
% \newenvironment{kframe}{%
%   \medskip{}
%   \setlength{\fboxsep}{.8em}
%   \def\at@end@of@kframe{}%
%   \ifinner\ifhmode%
%   \def\at@end@of@kframe{\end{minipage}}%
%   \begin{minipage}{\columnwidth}%
%   \fi\fi%
%   \def\FrameCommand##1{\hskip\@totalleftmargin \hskip-\fboxsep
%   \colorbox{shadecolor}{##1}\hskip-\fboxsep
%     % There is no \\@totalrightmargin, so:
%       \hskip-\linewidth \hskip-\@totalleftmargin \hskip\columnwidth}%
%   \MakeFramed {\advance\hsize-\width
%     \@totalleftmargin\z@ \linewidth\hsize
%     \@setminipage}}%
% {\par\unskip\endMakeFramed%
%   \at@end@of@kframe}
% \makeatother
% 
% \renewenvironment{Shaded}{\begin{kframe}}{\end{kframe}}

% Suppress underfull \hbox \vbox
\setlength{\hbadness}{1000pt}
\setlength{\vbadness}{1000pt}

% shut up overfull boxes

\hfuzz=5.002pt

% \tableofcontents becomes undefined. Use def from article.cls
% NO LONGER NEED THIS
% \makeatletter
% \renewcommand{\contentsname}{Contents}% For example
% \providecommand{\tableofcontents}{%
%   \section*{\contentsname}
%   \@starttoc{toc}
% }


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Index generation
% Indexentry for a word/phrase (Word inserted into the text)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% \usepackage{makeidx}
% \makeindex

\usepackage[small,firstabbrev]{authorindex}
\aimaxauthors{5}   % maximum number of authors to index
% \de{X} expands to d'X — used in French author names (e.g. d'Ocagne).
% In .Rnw this was defined via @Preamble in references.bib, but pandoc citeproc
% never emits @Preamble content to LaTeX, so it must be defined here instead.
\providecommand{\de}[1]{d'#1}

% Patch \@citex so that pandoc citeproc's \citeproc{key}{text} calls (which
% internally call \cite{key}) write \citationpage entries for the authorindex
% Perl script.  Without this, authorindex produces no entries because it only
% intercepts \aicite, not \cite.
% Also write \bibdata so the Perl script knows which .bib files to pass to BibTeX.
\AtBeginDocument{%
  % Disabled: \@citex patch causes "Extra }, or forgotten \endgroup" when
  % \citeproc (used by pandoc) is inside a \footnote{} on the first LaTeX pass.
  % index.ain is pre-built; authorindex does not need to re-run.
  %  \let\@ai@orig@citex\@citex
  %  \def\@citex[#1]#2{\@ai@orig@citex[#1]{#2}\@aicitey{#2}}%
  \if@filesw\immediate\write\@auxout{%
    \string\bibdata{references,R-refs,pkgs,packages,Rpackages-4.5.1}}\fi
  \includepdf[pages=1]{images/cover/cover-peng-scaled.pdf}
    }

% Pandoc's \citeproc{key}{text} uses \begingroup\cite{key}\endgroup.
% On the first LaTeX pass (or when .aux is stale from a failed build),
% undefined citations inside \footnote{} cause "Extra }, or forgotten \endgroup".
% \renewcommand does not reliably override \NewDocumentCommand-defined commands
% in modern LaTeX (xparse internal dispatch). Use \def at the TeX primitive level.
% Pandoc provides the correctly formatted text in #2 — \cite{#1} is redundant
% for Pandoc citeproc documents (no BibTeX step; bibliography is already in .tex).
%
% Write \citationpage{key}{page} to .aux for the authorindex Perl script.
% Calling \@aicitey directly causes "Undefined control sequence \@citeb->\@nil"
% due to \@for loop internals; writing to .aux directly avoids this entirely.
\def\citeproc#1#2{%
  \if@filesw\immediate\write\@auxout{\string\citationpage{#1}{\thepage}}\fi
  #2}

\newcommand{\IX}[1]{\index{#1}#1}
\newcommand{\ix}[1]{\index{#1}}
\newcommand{\ixmain}[1]{\index{#1|textbf}}
\newcommand{\ixon}[1]{\index{#1|(}}
\newcommand{\ixoff}[1]{\index{#1|)}}

% R functions — two args: {sort-key}{display-name}
% Caller includes () in both args; \ixfunc does NOT add them.
% Sort key: plain name with raw _ (OK for makeindex sorting; not typeset)
% Display:   name with \_ escaping so LaTeX can typeset it from the .ind file
% e.g. \ixfunc{stat_ellipse()}{stat\_ellipse()}
% Single-line definitions: parse-latex (pandoc filter) expands these macros during
% the PDF pandoc pass and inserts the body literally. Multi-line bodies with %
% at line-ends produce %.\footnote{} when a footnote follows — commenting out the
% footnote opener. Single-line bodies expand safely with no embedded %.
\newcommand{\ixfunc}[2]{\index{#1@\texttt{#2}}}

% R packages:  indexed under both package name and packages!
\newcommand{\ixp}[1]{\index{#1@\texttt{#1} package}\index{packages!#1@\texttt{#1}}}

% data sets: \ixd{name} — indexes under name and datasets!name, both with \texttt
\newcommand{\ixd}[1]{\index{#1@\texttt{#1} data}\index{datasets!#1@\texttt{#1}}}

% Index: see entries
\input{latex/indsee.tex} 

\urlstyle{tt}

%%%%%%%%%%%%%%%%%
% Subject index
\usepackage{imakeidx}
\makeindex[title=Subject Index,columns=2,intoc=true,options=-s latex/book.ist]

% Author index
% Author index: duplicate \usepackage removed (loaded above at line 136)


\usepackage{amsthm}
\makeatletter
\def\thm@space@setup{%
  \thm@preskip=8pt plus 2pt minus 4pt
  \thm@postskip=\thm@preskip
}
\makeatother

% Fix for modelsummary problem with \num{}
\renewcommand*{\num}[1]{#1}

% Fix TOC bug 1: prevent duplicate entries for unnumbered chapters.
% krantz \@schapter auto-adds \addcontentsline{toc}{fm}{} for every \chapter*;
% Quarto also emits \addcontentsline{toc}{chapter}{} for .unnumbered chapters.
% Removing the fm call here leaves only Quarto's {chapter} entry.
%
% Fix TOC bug 2: part headers drawn in wrong position.
% krantz \l@part defers rendering via \toc@draw: each part header is only
% drawn when the *next* \l@part fires, placing it after its own chapters.
% Adding \toc@draw at the top of \l@chapter flushes the pending header
% before the first chapter of each part, putting it in the right place.
\makeatletter
\def\@schapter#1{\if@twocolumn
                   \@topnewpage[\@makeschapterhead{#1}]%
                 \else
                   \@makeschapterhead{#1}%
                   \markboth{#1}{#1}%
                   \@afterheading
                 \fi}

\renewcommand*\l@chapter[2]{%
  \toc@draw
  \ifnum \c@tocdepth >\m@ne
    \addpenalty{-\@highpenalty}%
    \vskip 1.0em \@plus\p@
    \setlength\@tempdima{1.5em}%
    \begingroup
      \parindent \z@ \rightskip \@pnumwidth
      \parfillskip -\@pnumwidth
      \leavevmode \bfseries
      \advance\leftskip\@tempdima
      \hskip -\leftskip
      #1\nobreak\hfil \nobreak\hb@xt@\@pnumwidth{\hss #2}\par
          {\it\draw@authors}%
      \penalty\@highpenalty
    \endgroup
  \fi}
\makeatother

% This doesn't work
% l.300 \includegraphics
%                       [width=\textwidth]{images/cover-ellipse.jpg}

%\includegraphics[width=\textwidth]{images/cover-ellipse.jpg}

\frontmatter
  
%%%%%%%%%%%%%%%  End of preamble.tex %%%%%%%%%%%%%%%%%%%%  
%%%%%%%%%%%%%%%  ------------------- %%%%%%%%%%%%%%%%%%%%

# Task: Visibility and evaluation of code chunks

In Quarto and in knitr, it is possible to control whether R code in chunks is printed in the documuent or not (`echo=`)
and whether that code is actually run (`eval=`), using chunk options. However, the syntax used for Quarto and knitr
differ 